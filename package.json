{
  "name": "radar-extreme",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,vue,md}\""
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "npm@9.0.0"
}